---
segment:
  grid:
    description: make a grid layout.
    methods:
      float-minus-margin:
        cap:
      float-calc:
        cap:
      flex:
        cap:
      grid:
        cap: Firefox 52+, Chrome 57+, Safari 10.1+ Opera 44+
  gallery:
    methods:
      inline-block:
        cap:
      float:
        cap:
      grid:
        cap:
  media list:
    description: ways to make a media-list.
    methods:
      display-table:
        cap:
      float:
        cap:
  justify:
    description: make a list justify aligned.
    methods:
      text-align:
        cap:
  center:
    description: make element horizontally and vertically center aligned to its parent element.
    methods:
      pseudo-element:
        cpa:
  white space below image:
    description: There is white space below image, iframe, object and embed which break the layout.
    preview: true 
    methods:
      vertical-align: 
        cap: IE6
      display: 
        cap: IE7
